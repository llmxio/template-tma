// import { publicUrl } from "./public-url";

// Use the relative path since we have the manifest in the public folder
// export const MANIFEST_URL = publicUrl("tonconnect-manifest.json");

// This is the configuration for TonConnectUIProvider
export const tonConnectOptions = {
  manifestUrl: "https://static.llmx.io/tonconnect/llmx.io.json",
};
